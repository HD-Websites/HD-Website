import 'package:flutter/material.dart';
import 'package:webview_flutter/webview_flutter.dart';

void main() {
    runApp(
        MaterialApp(
            home: Scaffold(
                appbar: AppBar(title:Text("HD Websites")),
                body: Text("Harvil"),
            ),
        )
    );
} 